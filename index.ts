console.log("hello testing stack-attack");
console.log("first generating the sttack.config.json");

console.log("then use yarn cli <repoPath>");

function count(number) {
  console.log(number);
  number += 1;
}
count(1);
